#!/bin/sh

if [ "$LIBCLUSTER" = "1" ] || [ "$LIBCLUSTER" = "true" ]; then
  POD_A_RECORD=$(echo $K8_POD_IP | sed 's/\./-/g')
  domain=${POD_A_RECORD}.${K8_NAMESPACE}.pod.cluster.local
else
  domain=$(hostname -i)
fi

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@$domain
